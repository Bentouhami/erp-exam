// path: src/components/lists/AccountantList.tsx
'use client';

import UsersList from "@/components/lists/UsersList";

export default function AccountantList() {
    return (
        <UsersList role="ACCOUNTANT" />
    );
}
